export type Theme = {
  dark: boolean;
  colors: {
    primary: string;
    background: string;
    card: string;
    text: string;
    inactiveTint: string;
    activeTint: string;
    border: string;
    notification: string;
  };
};
