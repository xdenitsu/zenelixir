export type Theme = {
  type: string;
  colors: {
    primary: string;
    background: string;
    card: string;
    text: string;
    tint: string;
    activeTint: string;
    border: string;
    notification: string;
  };
};
